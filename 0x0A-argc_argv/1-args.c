#include "stdio.h"
/**
 * main - prints number of arguments, followed by a new line
 * @argc: int
 * @argv: list
 * Return: 0
 */
int main(int argc, char *argv[])
{
(void)argv;
printf("%d\n", argc);
return (0);
}
